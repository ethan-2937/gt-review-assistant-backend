<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jzyz.gtreviewassistant.mapper.StructureQualityDecisionMapper">
    <insert id="upsert" parameterType="StructureQualityDecision" useGeneratedKeys="true" keyProperty="id">
        insert into structure_quality_decision
        (project_id, issue_key, issue_type, severity, side, note_no, table_id, table_title, level, ref_id,
         decision, comment, reviewer, reviewed_at, created_at, updated_at)
        values
        (#{projectId}, #{issueKey}, #{issueType}, #{severity}, #{side}, #{noteNo}, #{tableId}, #{tableTitle}, #{level}, #{refId},
         #{decision}, #{comment}, #{reviewer}, #{reviewedAt}, #{createdAt}, #{updatedAt})
        on duplicate key update
            issue_type = values(issue_type),
            severity = values(severity),
            side = values(side),
            note_no = values(note_no),
            table_id = values(table_id),
            table_title = values(table_title),
            level = values(level),
            ref_id = values(ref_id),
            decision = values(decision),
            comment = values(comment),
            reviewer = values(reviewer),
            reviewed_at = values(reviewed_at),
            updated_at = values(updated_at)
    </insert>

    <select id="selectByProjectAndIssueKey" resultType="StructureQualityDecision">
        select *
        from structure_quality_decision
        where project_id = #{projectId}
          and issue_key = #{issueKey}
    </select>
</mapper>
